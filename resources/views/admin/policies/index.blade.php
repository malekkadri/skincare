@extends('admin.layouts.app') @section('title','Policies') @section('header','Policies')
@section('content')<a class="btn" href="{{ route('admin.policies.create') }}">Add Policy</a><div class="card"><table class="table"><tr><th>Title EN</th><th>Slug</th><th></th></tr>@foreach($items as $item)<tr><td>{{ $item->title_en }}</td><td>{{ $item->slug }}</td><td><a class="btn" href="{{ route('admin.policies.edit',$item) }}">Edit</a><form method="POST" action="{{ route('admin.policies.destroy',$item) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-danger">Delete</button></form></td></tr>@endforeach</table>{{ $items->links() }}</div>@endsection
