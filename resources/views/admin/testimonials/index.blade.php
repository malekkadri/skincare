@extends('admin.layouts.app') @section('title','Testimonials') @section('header','Testimonials')
@section('content')<a class="btn" href="{{ route('admin.testimonials.create') }}">Add Testimonial</a><div class="card"><table class="table"><tr><th>Name</th><th>Rating</th><th>Featured</th><th></th></tr>@foreach($items as $item)<tr><td>{{ $item->client_name }}</td><td>{{ $item->rating }}</td><td>{{ $item->is_featured ? 'Yes':'No' }}</td><td><a class="btn" href="{{ route('admin.testimonials.edit',$item) }}">Edit</a><form method="POST" action="{{ route('admin.testimonials.destroy',$item) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-danger">Delete</button></form></td></tr>@endforeach</table>{{ $items->links() }}</div>@endsection
