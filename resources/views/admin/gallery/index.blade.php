@extends('admin.layouts.app') @section('title','Gallery') @section('header','Gallery')
@section('content')<a class="btn" href="{{ route('admin.gallery.create') }}">Add Image</a><div class="card"><table class="table"><tr><th>Image</th><th>Title</th><th>Category</th><th></th></tr>@foreach($items as $item)<tr><td>@if($item->image_url)<img src="{{ $item->image_url }}" width="70">@endif</td><td>{{ $item->title_en }}</td><td>{{ $item->category }}</td><td><a class="btn" href="{{ route('admin.gallery.edit',$item) }}">Edit</a><form method="POST" action="{{ route('admin.gallery.destroy',$item) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-danger">Delete</button></form></td></tr>@endforeach</table>{{ $items->links() }}</div>@endsection
