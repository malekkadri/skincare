@extends('admin.layouts.app') @section('title','FAQ') @section('header','FAQ')
@section('content')<a class="btn" href="{{ route('admin.faq.create') }}">Add FAQ</a><div class="card"><table class="table"><tr><th>Question EN</th><th>Category</th><th></th></tr>@foreach($items as $item)<tr><td>{{ $item->question_en }}</td><td>{{ $item->category }}</td><td><a class="btn" href="{{ route('admin.faq.edit',$item) }}">Edit</a><form method="POST" action="{{ route('admin.faq.destroy',$item) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-danger">Delete</button></form></td></tr>@endforeach</table>{{ $items->links() }}</div>@endsection
